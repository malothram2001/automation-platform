# MongoDB Integration Dependencies
# Add these to your requirements.txt

# MongoDB driver for Python
pymongo==4.5.0

# Environment variable management (if not already installed)
python-dotenv==1.0.0

# FastAPI (should already have this)
fastapi>=0.104.0
uvicorn>=0.24.0

# Requests (should already have this)
requests>=2.31.0

# Pydantic (should already have this)
pydantic>=2.0.0